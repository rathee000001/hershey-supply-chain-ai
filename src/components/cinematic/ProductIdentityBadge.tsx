"use client";

type ProductIdentityBadgeProps = {
  variant?: "hero" | "floating";
};

/**
 * Temporarily disabled during Step 17E-B6F-R4A-2.
 *
 * The old badge rendered a small Target SKU card in the homepage hero.
 * The product identity will be rebuilt later as a proper right-side
 * product showcase using the real Hershey wrapper asset.
 */
export default function ProductIdentityBadge(_props: ProductIdentityBadgeProps) {
  return null;
}
